#!/usr/bin/env perl
use 5.12.0;
use lib '/Users/c9s/git/work/GitTools/git-release/Git-Release/lib';
use Git::Release;

my $manager = Git::Release->new;
$|++;

my $branch = shift;
my $ref = shift;
exit unless $branch;
$manager->create_feature_branch( $branch , $ref );
say 'Done';
